{
  "name": "urai-foundation-staff-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "bootstrap:owner": "node scripts/bootstrap-owner.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.0.1",
    "stripe": "22.3.2"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
